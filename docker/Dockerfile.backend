FROM node:22-alpine

WORKDIR /app

COPY server/package*.json ./
RUN npm install --production

COPY server/src ./src

# Seed the demo database at build time
RUN node src/database/seed.js

EXPOSE 3000

CMD ["node", "src/index.js"]
